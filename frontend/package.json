{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "apollo-client": "^1.9.2",
    "babel-plugin-transform-decorators-legacy": "^1.3.4",
    "graphql-tag": "^2.4.2",
    "midiconvert": "^0.4.1",
    "mobx": "^3.2.2",
    "mobx-react": "^4.2.2",
    "mobx-react-devtools": "^4.2.15",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-scripts": "1.0.10",
    "tone": "^0.10.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && rm build/static/js/*.map && rm build/static/css/*.map && rm -r ../static && mv build/* ../",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
