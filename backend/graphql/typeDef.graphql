type User {
    name: String!
    email: String!
    emailVerified: Boolean
    songs: [Song]
    stared: [Song]
    # setting: UserSetting
}

type Song {
    user: String!
    name: String!
    notes2D: [Notes1D]
    speed: Number
    stars: Number
}

